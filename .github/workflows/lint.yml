name: Code check

on:
  workflow_dispatch:
  push:
  pull_request:
    branches:
      - master

jobs:
  PylintCheck:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Set up Python 3.10
        uses: actions/setup-python@v3
        with:
          python-version: '3.10.x'

      - name: Install requirements
        run: |
          python -m pip install --upgrade pip
          pip install pylint

      - name: run with ls Local repo
        run: |
          pylint $(git ls-files '*.py')

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install --upgrade setuptools

      - name: run ls on local rep
        run: |
          ls -al


      - name: Running Pylint check
        run: |
          pylint $(git ls-files '*.py')

        
#                #NEW

      - name: Clone UAC_rover/poc-github-actions PUBLIC repository
        uses: GuillaumeFalourd/clone-github-repo-action@v2
        with:
           owner: 'Nergan123'
           repository: 'UAC_rover'

      - name: Access CLONED repository content
        run: |
          cd UAC_rover
          ls -la

      - name: Access LOCAL repository content
        run: |
          ls -la

      - name: Test with pytest
        run: | 
          python -m pip install pytest
          import UAC_rover
          pytest $(git ls-files 'test_calc_eff.py')

                                                #END


#      - name: Test with pytest (without extras; only package)
#        run: |
#          python -m pip install pytest
#          pytest $(git ls-files 'test_calc_eff.py')


#  Clone_UAC:
#    runs-on: ubuntu-latest
#
#    steps:
#
#     - name: Set up Python 3.10
#       uses: actions/setup-python@v3
#       with:
#         python-version: '3.10.x'
#
#     - name: Clone UAC_rover/poc-github-actions PUBLIC repository
#       uses: GuillaumeFalourd/clone-github-repo-action@v2
#       with:
#         owner: 'Nergan123'
#         repository: 'UAC_rover'
#
#     - name: Access cloned repository content
#       run: |
#         cd UAC_rover
#         ls -la
##
##     - name: pylint UAC_rover
##       run: |
##         python -m pip install pylint
##         cd UAC_rover/power_control
##         pylint SolarPanels.py
#
##     - name: Test with pytest (without extras; only package)
##       run: |
##          python -m pip install pytest
##          pytest $(git ls-files 'test_calc_eff.py')
#
#     - name: Install requirements
#       run: |
#         python -m pip install --upgrade pip
#         pip install pylint
#
##     - name: Running Pylint check
##       run: |
##         pylint SolarPanels.py
#
#     - name: Inside UAC_rover
#       run: |
#        cd UAC_rover/power_control
#        SolarPanels.py
#
#
#
#
