name: Code check

on:
  workflow_dispatch:
  push:
  pull_request:
    branches:
      - master

jobs:
  PylintCheck:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Set up Python 3.10
        uses: actions/setup-python@v3
        with:
          python-version: '3.10.x'

      - name: Install requirements
        run: |
          python -m pip install --upgrade pip
          pip install pylint
      - name: Running Pylint check
        run: |
          pylint $(git ls-files '*.py')
          
                #NEW

#      - name: Clone repo
#        uses: actions/checkout@v3

      - name: Clone UAC rover repository
        uses: actions/checkout@master
        with:
          repository: Nergan123/UAC_rover
          #token: $[{ secrets.my_pat ]}

      - run: |
         python -m pip install pytest
         pylint $(git ls-files '*.py')

      - uses: actions/checkout@v3               #Check where i am
        with:
          fetch-depth: 0

      - uses: actions/checkout@master
        with:
          repository: Nergan123/UAC_rover
          fetch-depth: 0
                                                #END
      - name: run with ls
        run: |
          pylint $(git ls-files '*.py')

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install --upgrade setuptools

#      - name: Test with pytest (without extras; only package)
#        run: |
#          python -m pip install pytest
#          pytest $(git ls-files 'test_calc_eff.py')

      - name: to home pc
        uses: actions/checkout@v2